<h1>Listing fantuans</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Creator</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @fantuans.each do |fantuan| %>
  <tr>
    <td><%= fantuan.name %></td>
    <td><%= fantuan.creator_id %></td>
    <td><%= link_to 'Show', fantuan %></td>
    <td><%= link_to 'Edit', edit_fantuan_path(fantuan) %></td>
    <td><%= link_to 'Destroy', fantuan, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add Fantuan', new_fantuan_path %>
